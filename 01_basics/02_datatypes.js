"use strict"; // treat all JS as newer version

// javascript engine's is inside the browser

// alert(3+3) we are using nodejs, not browser

console.log(3 
    +
    3) // code readability should be high

let name = "SAtty"
let
